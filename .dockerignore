# Keep the veepin build context small and deterministic. The root Dockerfile
# only needs the Go module source under cmd/, internal/, client/ (+ go.mod).
.git
dist/
bin/
nm/bin/
tests/interop/**/bin
*.deb
*.rpm
*.apk
